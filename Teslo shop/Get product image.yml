info:
  name: Get product image
  type: http

http:
  method: GET
  url: http://localhost:3000/api/files/product/:imageName
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
