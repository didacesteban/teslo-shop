info:
  name: Delete product
  type: http

http:
  method: DELETE
  url: http://localhost:3000/api/products/:id
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
